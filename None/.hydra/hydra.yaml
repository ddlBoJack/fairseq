hydra:
  run:
    dir: ???
  sweep:
    dir: ???
    subdir: ${hydra.job.config_name}__${hydra.job.override_dirname}
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    hydra_help: ???
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
  output_subdir: .hydra
  overrides:
    hydra: []
    task:
    - checkpoint.save_interval=40
    - checkpoint.keep_last_epochs=-1
    - checkpoint.no_epoch_checkpoints=false
    - checkpoint.save_interval_updates=0
    - checkpoint.keep_interval_updates=-1
    - checkpoint.save_dir=/home/zym22/model/multi2vec/hubert_baseline/checkpoint20/train_10h
    - task.data=/home/zym22/data/LibriSpeech/manifest/resource
    - task.label_dir=/home/zym22/data/LibriSpeech/manifest/resource
    - dataset.train_subset=train_10h
    - dataset.valid_subset=dev_other
    - dataset.num_workers=4
    - dataset.max_tokens=3200000
    - dataset.validate_interval=5
    - dataset.validate_after_updates=0
    - distributed_training.distributed_world_size=1
    - optimization.update_freq=[8]
    - model.w2v_path=/home/zym22/model/multi2vec/hubert_baseline/checkpoint20.pt
    - model.freeze_finetune_updates=0
    - common.log_interval=100
    - common.tensorboard_logdir=/home/zym22/model/multi2vec/hubert_baseline/checkpoint20/train_10h/tensorboard
    - common.log_file=/home/zym22/model/multi2vec/hubert_baseline/checkpoint20/train_10h/log/hydra_train.log
    - common.wandb_project=multi2vec
  job:
    name: hydra_train
    override_dirname: checkpoint.keep_interval_updates--1__checkpoint.keep_last_epochs--1__checkpoint.no_epoch_checkpoints-false__checkpoint.save_dir-/home/zym22/model/multi2vec/hubert_baseline/checkpoint20/train_10h__checkpoint.save_interval-40__checkpoint.save_interval_updates-0__common.log_file-/home/zym22/model/multi2vec/hubert_baseline/checkpoint20/train_10h/log/hydra_train.log__common.log_interval-100__common.tensorboard_logdir-/home/zym22/model/multi2vec/hubert_baseline/checkpoint20/train_10h/tensorboard__common.wandb_project-multi2vec__dataset.max_tokens-3200000__dataset.num_workers-4__dataset.validate_after_updates-0__dataset.validate_interval-5__distributed_training.distributed_world_size-1__model.freeze_finetune_updates-0__optimization.update_freq-[8]
    id: ???
    num: ???
    config_name: base_10h
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '-'
        item_sep: __
        exclude_keys:
        - run
        - task.data
        - task.label_dir
        - model.w2v_path
        - dataset.train_subset
        - dataset.valid_subset
        - criterion.wer_kenlm_model
        - criterion.wer_lexicon
  runtime:
    version: 1.0.7
    cwd: /home/zym22/fairseq
  verbose: false
